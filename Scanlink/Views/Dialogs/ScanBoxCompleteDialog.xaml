<Window x:Class="Scanlink.Views.Dialogs.ScanBoxCompleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="완료" Width="900" Height="960"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize">

    <Border Background="White" CornerRadius="8"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="28,24,28,16">
                    <!-- 체크 아이콘 -->
                    <Border Width="48" Height="48" CornerRadius="24"
                            Background="#EBF4EE" HorizontalAlignment="Center" Margin="0,0,0,14">
                        <TextBlock Text="✓" FontSize="22" FontWeight="Bold" Foreground="#1A7F37"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <TextBlock Text="스캔함이 추가되었습니다!" FontSize="15" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               HorizontalAlignment="Center" Margin="0,0,0,6"/>

                    <TextBlock Text="복합기 패널에서 스캔함을 확인하세요." FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" Margin="0,0,0,16"/>

                    <!-- 매뉴얼 이미지 -->
                    <Border x:Name="ManualImageBorder" CornerRadius="6" Margin="0,0,0,8"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                            Visibility="Collapsed">
                        <Image x:Name="ManualImage" Stretch="Uniform"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- 하단 버튼 -->
            <Border Grid.Row="1" Padding="28,12"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                <Button Content="확인" Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Stretch" Click="OkButton_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>
