<UserControl x:Class="Scanlink.Views.Pages.ScanBoxListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16,12,16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 뒤로가기 -->
        <Button Grid.Row="0" Content="← 복합기 목록" Style="{StaticResource LinkButton}"
                HorizontalAlignment="Left" Margin="0,0,0,8" FontSize="12"
                Command="{Binding GoBackCommand}"/>

        <!-- 헤더 -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{Binding Device.DisplayName}" FontSize="15" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0">
                    <Run Text="IP:"/>
                    <Run Text="{Binding Device.Ip, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
            <Button Content="+ 스캔함" Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Command="{Binding AddScanBoxCommand}" Padding="14,0" FontSize="12"
                    VerticalContentAlignment="Center"/>
        </Grid>

        <!-- 빈 상태 -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding HasScanBoxes, Converter={StaticResource InverseBoolToVis}}">
            <TextBlock Text="📂" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <TextBlock Text="등록된 스캔함이 없습니다." FontSize="13"
                       Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="우측 상단 버튼으로 스캔함을 추가하세요." FontSize="11"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- 스캔함 목록 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding HasScanBoxes, Converter={StaticResource BoolToVis}}">
            <ItemsControl ItemsSource="{Binding ScanBoxes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10,8" Margin="0,0,0,4" CornerRadius="6" Cursor="Hand"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding DataContext.SelectScanBoxCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 이름 + 자물쇠 -->
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                    <TextBlock Text="🔒" FontSize="10" Margin="6,0,0,0" VerticalAlignment="Center"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Visibility="{Binding Password, Converter={StaticResource StringToVisibility}}"/>
                                </StackPanel>

                                <!-- 경로 열기 + 삭제 -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="📁" Style="{StaticResource IconButton}" ToolTip="경로 열기"
                                            Width="26" Height="26" FontSize="12"
                                            Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="✕" Style="{StaticResource IconButton}" ToolTip="삭제"
                                            Width="26" Height="26" FontSize="11"
                                            Command="{Binding DataContext.DeleteScanBoxCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 삭제 로딩 오버레이 -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="#80FFFFFF" CornerRadius="4"
                Visibility="{Binding IsDeleting, Converter={StaticResource BoolToVis}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="120" Height="3"
                             Foreground="{StaticResource PrimaryBrush}" Background="#E8EAF0"
                             BorderThickness="0"/>
                <TextBlock Text="삭제 중..." FontSize="12" Margin="10,0,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
