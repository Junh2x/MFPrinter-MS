<Window x:Class="JaScan.Views.Dialogs.ScanBoxCompleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="완료" Width="320" Height="260"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize">

    <Border Background="White" CornerRadius="8" Padding="24,20"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <StackPanel VerticalAlignment="Center">
            <Border Width="48" Height="48" CornerRadius="24"
                    Background="#EBF4EE" HorizontalAlignment="Center" Margin="0,0,0,14">
                <TextBlock Text="✓" FontSize="22" FontWeight="Bold" Foreground="#1A7F37"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <TextBlock Text="스캔함이 추가되었습니다!" FontSize="14" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       HorizontalAlignment="Center" Margin="0,0,0,6"/>

            <TextBlock Text="복합기 패널에서 스캔함을 확인하세요." FontSize="11"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" Margin="0,0,0,6"/>

            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="5"
                    Height="40" Margin="0,4,0,16">
                <TextBlock Text="📖 매뉴얼 이미지 (추후 추가)" FontSize="10"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <Button Content="확인" Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Center" Padding="32,7"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Border>
</Window>
